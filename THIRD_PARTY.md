# Prior art

The interception/approval idea was inspired by the MIT-licensed
`clawnify/agent-permissions` project. This implementation uses a new structured
policy engine rather than its `Tool(content)` rule parser and does not expose its
self-modifying permission tools.

Upstream: https://github.com/clawnify/agent-permissions
