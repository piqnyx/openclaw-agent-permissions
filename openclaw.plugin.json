{
  "id": "agent-permissions",
  "name": "Agent Permissions Policy",
  "description": "Fail-closed filesystem, exec-regex, and generic-tool permission policy for sandboxed OpenClaw agents.",
  "version": "2.0.5",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "policyPath": {
        "type": "string",
        "default": "/home/openclaw/.openclaw/permissions.json",
        "description": "Host-side structured policy file. Keep it outside agent workspaces."
      },
      "virtualWorkspaceRoot": {
        "type": "string",
        "default": "/workspace",
        "description": "Sandbox-visible workspace root used to resolve relative file paths."
      },
      "approvalTimeoutMs": {
        "type": "number",
        "minimum": 1000,
        "maximum": 600000,
        "default": 540000
      },
      "logDecisions": {
        "type": "boolean",
        "default": true
      },
      "failClosed": {
        "type": "boolean",
        "default": true,
        "description": "Block tool calls when policy loading/evaluation fails."
      }
    }
  }
}
